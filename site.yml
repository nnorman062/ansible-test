---
# CentOS 7 LAMP Stack
# Version: 1.0

- name: deploy MySQL and configure databases
  hosts: dbservers
  remote_user: root
   
  roles:
   - db
   
- name: deploy Apache, PHP and configure website code
  hosts: webservers
  remote_user: root
   
  roles:
   - web
